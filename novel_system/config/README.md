Configuration files for the novel system live here.

- Environment variables are loaded from the project root `.env`.
- Database migrations, secrets management, and environment-specific overrides can be added later.
